# 디스코드 출석체크 봇 — 설정 & 오라클 배포 가이드

10~20명 서버용 출석체크 봇입니다. 지정한 음성채널 2개에 **입장/퇴장할 때마다 스탬프**가 찍히고, **매주 수요일 오후 5시에 정산**(누적 시간 순위 + 출석 O/X 명단)이 전송됩니다.

---

## 0. 가장 먼저 — 토큰 재발급 ⚠️

기존 코드에 토큰이 그대로 노출됐기 때문에 이미 유출된 것으로 봐야 합니다.

1. https://discord.com/developers/applications 접속
2. 본인 애플리케이션 → **Bot** 메뉴 → **Reset Token** 클릭
3. 새로 나온 토큰을 복사 (이 값은 코드에 직접 넣지 말고 아래 `.env`에만 넣습니다)

---

## 1. 봇 권한 & 인텐트 설정

Developer Portal의 **Bot** 메뉴에서 아래 3개를 모두 **켜세요** (Privileged Gateway Intents):

- PRESENCE INTENT (선택)
- **SERVER MEMBERS INTENT** ✅ (출석 명단에 필요)
- **MESSAGE CONTENT INTENT** ✅ (`!현황` 명령어에 필요)

**OAuth2 → URL Generator** 에서 scope는 `bot`, 권한은 `View Channels`, `Send Messages`, `Read Message History`, `Connect`(음성 상태 조회) 정도를 체크한 뒤 생성된 링크로 봇을 서버에 초대합니다.

---

## 2. 코드 안의 설정값 바꾸기

`attendance_bot.py` 상단 설정 부분을 본인 서버에 맞게 수정하세요.

```python
LOG_CHANNEL_NAME = "기록"                    # 스탬프가 찍힐 텍스트 채널 이름
VOICE_CHANNEL_NAMES = ["정모방", "스터디방"]   # 감지할 음성채널 2개의 실제 이름
MIN_ATTENDANCE_MINUTES = 1                   # 이 시간 이상 머물러야 '출석 O' 인정
```

- `LOG_CHANNEL_NAME` 채널은 서버에 미리 만들어 두세요.
- `VOICE_CHANNEL_NAMES` 는 서버에 존재하는 음성채널 이름과 **정확히** 일치해야 합니다.
- 잠깐 들렀다 나간 것도 출석으로 치고 싶지 않다면 `MIN_ATTENDANCE_MINUTES` 를 예: `10` 으로 올리세요.

---

## 3. 내 PC에서 먼저 테스트 (권장)

배포 전에 로컬에서 한 번 돌려보는 걸 추천합니다.

```bash
pip install discord.py pytz python-dotenv
```

같은 폴더에 `.env` 파일을 만들고:

```
DISCORD_TOKEN=여기에_2번에서_받은_새_토큰
```

실행:

```bash
python attendance_bot.py
```

콘솔에 `봇 시작됨: ...` 이 뜨면 성공. 음성채널에 들어갔다 나오면서 `기록` 채널에 스탬프가 찍히는지, `!현황` 명령어가 동작하는지 확인하세요.

---

## 4. 오라클 클라우드 무료 티어에 24시간 배포

### 4-1. 인스턴스 생성
1. 오라클 클라우드 콘솔 로그인 → **Compute → Instances → Create Instance**
2. 이미지: **Canonical Ubuntu 22.04** (또는 24.04)
3. Shape: **Always Free** 표시가 있는 것 선택
   - `VM.Standard.A1.Flex` (ARM, OCPU 1~2 / 메모리 6~12GB) 추천 — 무료 한도 안에서 넉넉함
   - A1이 용량 부족(Out of capacity)으로 안 잡히면 `VM.Standard.E2.1.Micro`(AMD)로 대체 가능
4. **SSH 키**: "Generate a key pair for me" 로 개인키(.key)를 **반드시 다운로드**해 두세요
5. Create 후 인스턴스의 **Public IP** 를 메모

### 4-2. 접속
```bash
# 다운로드한 키 권한 설정 (Mac/Linux)
chmod 400 ~/Downloads/ssh-key-xxxx.key

# 접속 (Ubuntu 계정)
ssh -i ~/Downloads/ssh-key-xxxx.key ubuntu@<Public_IP>
```
> Windows는 PowerShell에서 같은 `ssh` 명령을 쓰거나 PuTTY를 사용하면 됩니다.

### 4-3. 파이썬 환경 준비
```bash
sudo apt update && sudo apt install -y python3 python3-pip python3-venv
mkdir ~/bot && cd ~/bot
python3 -m venv venv
source venv/bin/activate
pip install discord.py pytz python-dotenv
```

### 4-4. 파일 올리기
`attendance_bot.py` 를 서버로 복사합니다. (내 PC 터미널에서 실행)
```bash
scp -i ~/Downloads/ssh-key-xxxx.key attendance_bot.py ubuntu@<Public_IP>:~/bot/
```
서버 안에서 `.env` 파일을 직접 만들어 토큰을 넣습니다.
```bash
cd ~/bot
nano .env
# 아래 한 줄 입력 후 Ctrl+O, Enter, Ctrl+X
# DISCORD_TOKEN=여기에_새_토큰
```

### 4-5. systemd 로 24시간 자동 실행 등록
재부팅되거나 봇이 죽어도 자동으로 다시 켜집니다.

```bash
sudo nano /etc/systemd/system/discordbot.service
```

아래 내용 붙여넣기:

```ini
[Unit]
Description=Discord Attendance Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/bot
ExecStart=/home/ubuntu/bot/venv/bin/python /home/ubuntu/bot/attendance_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

등록 & 시작:
```bash
sudo systemctl daemon-reload
sudo systemctl enable discordbot
sudo systemctl start discordbot
```

### 4-6. 상태 확인 / 로그 보기
```bash
sudo systemctl status discordbot      # 실행 상태
journalctl -u discordbot -f           # 실시간 로그 (Ctrl+C로 종료)
```

코드를 수정한 뒤 다시 올렸다면:
```bash
sudo systemctl restart discordbot
```

---

## 5. 참고 사항

- **데이터 보존**: 주간 기록은 `~/bot/attendance_data.json` 에 저장되어 봇이 재시작돼도 그 주 집계가 유지됩니다. 봇이 꺼져 있는 동안 오간 사람은 감지 못 하니, 되도록 계속 켜 두세요.
- **정산 타이밍**: 수요일 오후 5시 정각에 자동 전송됩니다. 이 시점에 봇이 꺼져 있으면 그 주 정산은 스킵되니 주의하세요.
- **아웃바운드 방화벽**: 오라클 인스턴스는 기본적으로 바깥으로 나가는(디스코드 접속) 통신이 열려 있어 별도 인바운드 포트 개방은 필요 없습니다.
- **비용**: 위 구성은 오라클 Always Free 한도 내이며 요금이 청구되지 않습니다. (단, 계정에 카드 등록은 되어 있어야 함)
