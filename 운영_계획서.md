# 디스코드 출석 봇 — 운영 계획서

봇을 **안정적으로 배포**하고, **남의 서버에서 문제없이 운영**하고, **깃헙으로 기능을 추가**하며, **나중에 다른 사람에게 넘겨줄** 수 있도록 하는 전체 로드맵입니다.

전체 흐름 한눈에:

```
0. 토큰 재발급 & 계정 준비
        ↓
1. 깃헙에 코드 올리기 (버전관리 기반)
        ↓
2. 서버 주인과 협의 + 봇 초대 (권한 문제 해결)
        ↓
3. 오라클 서버에 24시간 배포
        ↓
4. 운영 & 기능 추가 워크플로우
        ↓
5. 나중에 다른 사람에게 인수인계
```

---

## Phase 0. 준비 — 토큰 재발급 & 계정 (가장 먼저) ⚠️

기존 토큰은 이미 노출됐으니 **폐기하고 새로 발급**합니다.

1. https://discord.com/developers/applications 접속 → 내 봇 애플리케이션 선택
2. **Bot** 메뉴 → **Reset Token** → 새 토큰 복사
3. 이 토큰은 **절대 코드나 깃헙에 넣지 않음.** 오직 서버의 `.env` 파일에만 저장.
4. 준비물 체크
   - [ ] 깃헙(GitHub) 계정 (없으면 https://github.com 에서 가입)
   - [ ] 오라클 클라우드 계정 (이미 만들어 둠 ✅)
   - [ ] 봇 초대 권한 — 대상 서버의 **주인 또는 관리자**의 협조

> 핵심 원칙: **비밀값(토큰)은 코드와 분리한다.** 그래야 코드를 깃헙에 공개해도, 남에게 넘겨도 안전합니다.

---

## Phase 1. 깃헙에 코드 올리기 (기능 추가·인수인계의 기반)

깃헙을 쓰면 (1) 코드 이력이 남고 (2) 어디서든 내려받아 배포하고 (3) 나중에 접근 권한만 넘기면 인수인계가 끝납니다.

### 1-1. 저장소(repo) 만들기
- 깃헙에서 **New repository** → 이름 예: `discord-attendance-bot`
- **Private(비공개) 권장** (굳이 공개할 이유 없음)

### 1-2. 비밀값·데이터는 깃에 올리지 않기 — `.gitignore`
저장소에 아래 내용의 `.gitignore` 파일을 둡니다.

```
.env
attendance_data.json
__pycache__/
*.pyc
venv/
```

### 1-3. 올릴 파일 구성
```
discord-attendance-bot/
├── attendance_bot.py      # 봇 코드
├── requirements.txt       # 필요한 패키지 목록
├── .gitignore
├── .env.example           # 토큰 없이, 형식만 보여주는 예시
└── README.md              # 설치·실행법 (배포_가이드 내용)
```
- `requirements.txt`:
  ```
  discord.py
  pytz
  python-dotenv
  ```
- `.env.example`:
  ```
  DISCORD_TOKEN=여기에_토큰
  ```

### 1-4. 코드에서 테스트 토큰 제거
운영 단계에선 `attendance_bot.py`의 `TEST_TOKEN` 줄을 지우고 `.env`만 쓰도록 되돌립니다.
```python
TOKEN = os.getenv("DISCORD_TOKEN")
```

### 1-5. 올리기 (로컬 터미널)
```bash
cd discord-attendance-bot
git init
git add .
git commit -m "최초 커밋: 출석 봇"
git branch -M main
git remote add origin https://github.com/<내아이디>/discord-attendance-bot.git
git push -u origin main
```

---

## Phase 2. 남의 서버에서 운영하기 (권한 문제)

**핵심: 봇 앱은 "내 것"이고, 서버는 "그 사람 것"이어도 전혀 문제없습니다.** 봇은 서버에 *초대되어* 동작하는 구조라, 내가 서버 주인일 필요가 없어요. 다만 아래를 서버 주인과 맞춰야 합니다.

### 2-1. 봇 초대 (주인 협조 필요)
- 봇을 서버에 추가하려면 **"서버 관리(Manage Server)" 권한**이 필요합니다.
- 방법 A: 내가 만든 **초대 링크**를 서버 주인에게 전달 → 주인이 클릭해서 승인
- 방법 B: 주인이 나에게 관리자 권한을 주고, 내가 직접 초대
- 초대 링크 만들기: Developer Portal → **OAuth2 → URL Generator**
  - Scopes: `bot`
  - Bot Permissions: `View Channels`, `Send Messages`, `Read Message History`, `Connect`

### 2-2. 봇이 채널을 "볼 수 있게" 하기
봇은 자기가 **볼 수 있는** 채널만 감지/기록합니다. 서버 주인에게 요청할 것:
- 봇 역할(role)이 감지할 **음성채널**(`공부방` 등)을 볼 수 있어야 함 → 입장/퇴장 감지에 필수
- 봇 역할이 **`기록` 텍스트 채널**에서 메시지를 보낼 수 있어야 함 → 스탬프 전송에 필수

### 2-3. 채널 이름 변경 리스크 → **채널 ID로 고정 권장**
남의 서버라 주인이 채널 이름을 바꾸면 이름 기반 감지가 깨집니다. 그래서 이름 대신 **채널 ID**로 지정하는 게 안전합니다.
1. 디스코드 설정 → 고급 → **개발자 모드 ON**
2. 대상 음성채널 우클릭 → **채널 ID 복사**
3. 코드에 입력:
   ```python
   VOICE_CHANNEL_IDS = [111111111111, 222222222222]
   ```
- (참고) 서버 주인과 "이 채널들은 봇용이니 이름/권한 바꿀 때 알려달라"고 미리 합의해두면 좋습니다.

### 2-4. 운영 중 주의
- 주인이 봇을 추방하면 당연히 멈춤 → 주인과 신뢰관계 필요
- 봇 데이터(`attendance_data.json`)는 **내 오라클 서버**에 쌓임 → 서버 주인이 아니라 내가 관리

---

## Phase 3. 오라클 클라우드 무료 티어에 24시간 배포

> 상세 명령어는 함께 드린 **`배포_가이드.md`** 참고. 여기서는 큰 흐름만.

1. **인스턴스 생성**: Ubuntu 22.04, `VM.Standard.A1.Flex`(Always Free). SSH 키 다운로드 필수.
2. **접속**: `ssh -i 키파일 ubuntu@<PublicIP>`
3. **환경 준비**:
   ```bash
   sudo apt update && sudo apt install -y python3-pip python3-venv git
   git clone https://github.com/<내아이디>/discord-attendance-bot.git
   cd discord-attendance-bot
   python3 -m venv venv && source venv/bin/activate
   pip install -r requirements.txt
   ```
4. **토큰 넣기**: 서버에서 `.env` 직접 생성 (`nano .env` → `DISCORD_TOKEN=...`)
5. **자동 실행 등록(systemd)**: 죽어도/재부팅돼도 자동 재시작 (가이드의 서비스 파일 사용)
   ```bash
   sudo systemctl enable discordbot && sudo systemctl start discordbot
   ```
6. **확인**: `journalctl -u discordbot -f` 로 실시간 로그 확인

---

## Phase 4. 운영 & 기능 추가 워크플로우 (깃헙 활용)

한 번 깃헙+오라클로 세팅하면, 기능 추가는 아래 루프가 전부입니다.

```
내 PC에서 수정 → 로컬 테스트 → 깃헙 push → 오라클에서 pull → 서비스 재시작
```

구체적으로:
1. **내 PC**에서 `attendance_bot.py` 수정하고 로컬에서 `python attendance_bot.py`로 테스트
2. 잘 되면 깃헙에 반영:
   ```bash
   git add . && git commit -m "기능: OOO 추가" && git push
   ```
3. **오라클 서버**에서 받아서 재시작:
   ```bash
   cd ~/discord-attendance-bot && git pull
   sudo systemctl restart discordbot
   ```

**추가하기 좋은 기능 예시** (원하면 언제든 요청):
- `!리셋` : 주간 데이터 수동 초기화 (관리자 전용)
- `!내순위` : 본인 이번 주 순위/시간 조회
- 자정에 개인별 "오늘의 공부시간" 자동 요약 전송
- 정모 참석 여부를 이모지 반응으로 체크
- 월간 리포트

---

## Phase 5. 나중에 다른 사람에게 인수인계

인수인계는 **세 가지 자산**을 넘기는 일입니다: ① 봇 앱 ② 코드 ③ 호스팅. 각각 방법이 다릅니다.

### ① 봇 애플리케이션 (Discord) — "팀(Team)" 사용 권장
개인 계정에 묶인 봇은 그 계정 없이는 못 넘깁니다. 그래서 **Developer Team**으로 옮겨두면 사람만 교체하면 됩니다.
1. Developer Portal → **Teams** → 팀 생성
2. 내 봇 애플리케이션을 그 팀 소유로 이전 (Transfer to Team)
3. 인수인계 시: 팀에 새 담당자를 **멤버로 추가** (또는 팀 소유권 이전)
- 넘긴 뒤 새 담당자가 **토큰을 재발급**하면 이전 담당자는 접근 불가 → 깔끔한 인계

### ② 코드 (GitHub)
- 저장소 **Settings → Collaborators** 에서 새 담당자 추가
- 또는 저장소 소유권 이전(Transfer ownership)

### ③ 호스팅 (Oracle)
- 방법 A (권장·깔끔): 새 담당자가 **자기 오라클 무료 티어**에 README 보고 새로 배포. 데이터 파일(`attendance_data.json`)만 복사해 넘기면 기록도 유지.
- 방법 B: 기존 인스턴스 접근권(SSH 키/오라클 콘솔)을 넘김 — 계정이 얽혀 덜 깔끔함.

### 인수인계 체크리스트
- [ ] GitHub 저장소 접근 권한 부여
- [ ] Discord 봇을 Team으로 이전 + 새 담당자 추가
- [ ] 새 담당자가 토큰 재발급 → 서버 `.env` 갱신 → 서비스 재시작
- [ ] 호스팅 이전(새 배포 또는 접근권 이전) + 데이터 파일 인계
- [ ] 서버 주인에게 담당자 변경 공지

---

## 요약 체크리스트

- [ ] Phase 0: 토큰 재발급, 깃헙 계정 확인
- [ ] Phase 1: 깃헙 저장소 생성 + `.gitignore`로 비밀값 분리 + push
- [ ] Phase 2: 서버 주인과 협의, 봇 초대, 채널 ID 고정
- [ ] Phase 3: 오라클 인스턴스 생성 → git clone → .env → systemd 등록
- [ ] Phase 4: (수정→push→pull→restart) 루프로 기능 추가
- [ ] Phase 5: Team 전환·GitHub 권한·호스팅 이전으로 인수인계
