# 스터디 봇 — 개발 & 유지보수 문서

디스코드 스터디 출석·타임랭킹 봇의 구조/기능/운영/트러블슈팅 정리.
※ 토큰·SSH키·서버 IP 등 민감정보는 이 문서에 넣지 않음(자리표시자).
※ 변경 이력은 `PATCHNOTES.md` 참고.

---

## 1. 개요
- **목적**: `공부방` 음성채널 출석·공부시간 자동 기록 + 주간/월간 타임랭킹.
- **규모**: 10~20명 스터디 서버.

## 2. 구성 (아키텍처)
```
[디스코드 서버] ←→ [봇 앱(토큰)] ←→ [오라클 VM에서 24시간 실행되는 파이썬 봇]
                                       └ 코드: GitHub (git pull로 갱신)
                                       └ 토큰: 서버의 .env 에만
                                       └ 데이터: attendance_data.json (서버 로컬, git 제외)
                                       └ 기간 로그: logs/*.md (git 제외)
```
- **저장소**: `https://github.com/sillvi3d/discord-attendance-bot` (Public, 비밀값 없음)
- **로컬 작업 폴더**: `D:\99_Discord-Bot\discord-attendance-bot`
- **테스트 폴더**: `D:\99_Discord-Bot\discord-attendance-bot-test` (별도 테스트 봇 토큰으로 로컬 실행)
- **호스팅**: 오라클 무료 티어, Ubuntu 22.04, `VM.Standard.E2.1.Micro`
- **접속**: `ssh -i "<SSH키_경로>" ubuntu@<서버_공인_IP>`
- **자동 실행**: systemd 서비스 `discordbot`

## 3. 핵심 설정값 (`attendance_bot.py` 상단)
| 변수 | 의미 | 현재값 |
|---|---|---|
| `LOG_CHANNEL_NAME` | 스탬프/정산 채널 (이름 '포함' 매칭) | `"출석체크"` |
| `BOT_NICKNAME` | 서버 표시 별명 | `"스터디 봇"` |
| `VOICE_CHANNEL_NAMES` | 감지 음성채널 키워드('포함' 매칭) | `["공부방"]` |
| `VOICE_CHANNEL_IDS` | 채널 ID 지정(비우면 이름 사용) | `[]` |
| `COLOR_*` | 임베드 카드 색상 | 0xRRGGBB |

- 채널 감지는 **이름 포함 매칭**: `공부방🔇`, `🎧공부방`, `👆출석체크` 등 이모지가 붙어도 인식.

## 4. 집계 규칙 (중요)
- **세션 저장**: 퇴장 시 `{uid, name, start, end, dur}` 를 영구 로그에 append.
- **기간 랭킹(일/주/월)**: 그 기간에 **실제로 걸친 시간만** 합산(경계에서 분할). 자정/수요일 경계를 걸친 세션은 각 기간에 해당 몫만 들어감.
- **퇴장 카드 / `!오늘`**:
  - **머문 시간** = 그 세션의 **전체 시간** (예: 22:00~00:30 → 2시간 30분)
  - **오늘 누적** = **오늘 몫만** (자정 기준, 위 예시면 30분)
- **주 시작 기준**: 매주 수요일 17:00. **월 시작**: 매월 1일 00:00.

## 5. 기능 & 명령어
**자동 발표**
- 매일 07:00 — `☀️ 어제의 스터디 랭킹` (전날 0시~23:59:59)
- 매주 수 17:00 — `🏆 주간 랭킹` + Markdown 로그 첨부 (지난 회의 참석자 포함)
- 매월 말일 21:00 — `📅 월간 랭킹` + 로그 첨부
- 매월 1일 00:00 — 월 구분선

**명령어**
| 명령어 | 설명 | 권한 |
|---|---|---|
| `!주간`/`!현황` | 이번 주 전체 랭킹 | 누구나 |
| `!월간` | 이번 달 전체 랭킹 | 누구나 |
| `!오늘` | 내 오늘 기록 | 누구나 |
| `!내주간` | 내 이번 주 누적 | 누구나 |
| `!내순위` | 내 이번 주 순위 | 누구나 |
| `!회의현황` | 오늘 회의 참석자 | 누구나 |
| `!주간로그`/`!월간로그`/`!전체로그`/`!회의로그` | 기록 .md 파일 | 누구나 |
| `!구분선` | 월 구분선 수동 게시 | 누구나 |
| `!회의체크` | 회의 참석 체크 게시(✅ 집계) | 관리자 |
| `!시간추가 @멤버 분` | 시간 수동 보정(음수 가능) | 관리자 |

- (구) `!정모체크`/`!정모현황` 은 별칭으로 계속 작동.

## 6. 회의 참석 로직
- `!회의체크`(관리자) → "오늘 회의 참석" 체크 메시지 게시, ✅ 반응으로 집계.
- 새 체크를 시작하면 직전 회의 기록은 `meeting_log`(이력)에 보관.
- 주간 발표(수 17시)는 **그 기간에 있었던 회의 참석자**를 표시 → 실질적으로 "지난 회의 참석자"가 뜸. 그 주에 체크 안 했으면 "없음".
- `!회의로그` 로 회의 참석 이력을 .md 로 받음.

## 7. 유지보수 워크플로우
```
내 PC 수정 → 로컬 테스트(테스트 봇) → git push → 서버 git pull → 서비스 재시작
```
1. 내 PC (`D:\99_Discord-Bot\discord-attendance-bot`): `git add . && git commit -m "..." && git push`
2. 서버: `cd ~/discord-attendance-bot && git pull && sudo systemctl restart discordbot`
3. **변경할 때마다 `PATCHNOTES.md` 에 기록** 후 함께 커밋.

## 8. 서버 운영 명령어
```bash
ssh -i "<SSH키_경로>" ubuntu@<서버_공인_IP>   # 접속
sudo systemctl status discordbot              # 상태
journalctl -u discordbot -f                   # 실시간 로그 (Ctrl+C)
sudo systemctl restart discordbot             # 재시작
nano ~/discord-attendance-bot/.env            # 토큰 교체 후 restart
```

## 9. 테스트 환경(개인 서버)
- 별도 **테스트 봇(새 토큰)** 을 Developer Portal에서 만들고 개인 서버에 초대.
- `discord-attendance-bot-test/.env` 에 테스트 토큰 입력 → `python attendance_bot.py` 로 로컬 실행.
- ⚠️ 같은 토큰은 동시에 한 곳에서만 실행 가능 (본 서버 봇과 충돌 주의).

## 10. 트러블슈팅 로그
| 증상 | 원인 | 해결 |
|---|---|---|
| 감지·명령어 전부 안 됨 | Privileged Intents 미활성 | Portal에서 SERVER MEMBERS/MESSAGE CONTENT INTENT ON |
| 스탬프 안 뜸 | 채널명 불일치(이모지 등) | 이름 '포함' 매칭 / `VOICE_CHANNEL_IDS` 사용 |
| 자정 걸친 세션 시간 이상 | 오늘 몫만 표시하던 버그 | 머문시간=세션전체 / 오늘누적=오늘몫 분리 |
| 모바일 랭킹 이름 이상(@id) | 멘션 렌더 차이 | 서버 별명을 텍스트로 직접 표시 |
| Ampere shape 없음 | 무료 ARM 소진 | E2.1.Micro(AMD) 사용 |
| 공인 IP 토글 회색 | 생성 마법사 버그 | 생성 후 VNIC→Edit→Ephemeral public IP |
| SSH UNPROTECTED KEY | Windows 키 권한 과다 | `icacls "<키>" /inheritance:r /grant:r "%USERNAME%:R"` |
| `sudo apt` 안 됨 | Windows에서 실행 | SSH 접속 후 서버에서 실행 |
| cmd 붙여넣기 `^V` | Ctrl+V 미지원 | 마우스 우클릭 붙여넣기 |
| private repo clone 인증 | 서버 인증 없음 | repo Public 전환(비밀값 없음) 또는 PAT |

## 11. 보안 체크리스트
- [x] 토큰은 서버 `.env` 에만, 코드/깃/히스토리에 없음
- [x] `attendance_data.json`, `logs/` git 제외
- [x] SSH 키 볼트 밖 보관 + 권한 잠금
- [ ] 봇 초대 시 **관리자 권한 주지 않기** (최소 권한만) ← 멤버 보호 핵심
- [ ] 토큰을 스크린샷/텍스트파일에 남기지 않기

## 12. 관련 문서
- `PATCHNOTES.md` — 변경 이력
- `README.md` — 요약
- `배포_가이드.md` — 오라클 배포 상세
- `운영_계획서.md` — 로드맵 & 인수인계
